interface:
  display_name: "Godspeed Tasks"
  short_description: "Generic Godspeed discovery, inbox triage, and API-backed task organization."
  default_prompt: "Use $godspeed-tasks to inspect or organize Godspeed through the API, keep public repo guidance generic, use explicit writes for objective edits, and require preview or approval before bulk or heuristic categorization."
