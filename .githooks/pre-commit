#!/usr/bin/env bash

set -euo pipefail

if [[ "${SKIP_CODEX_SYNC_VALIDATE:-0}" == "1" ]]; then
    exit 0
fi

REPO_ROOT="$(git rev-parse --show-toplevel)"
exec "$REPO_ROOT/scripts/validate-codex-sync.sh"
