#!/usr/bin/env bash

set -euo pipefail

repo_root="$(git rev-parse --show-toplevel)"

if ! "$repo_root/scripts/run-overlay-git-hook.sh" pre-push "$@"; then
    printf '[dotfiles-pre-push] Failed to run overlay pre-push hooks\n' >&2
fi

exit 0
